# AI Personal OS — Phase 1 dependencies
#
# Dependencies are added per Build Plan milestone, only when a ticket needs
# them (a PDF parser at T2.1, lancedb at T2.4, etc.).

# T1.2 — folder watching
watchdog>=4.0

# T2.2 — PDF text extraction
pypdf>=4.0

# T2.5 — local embeddings via Ollama (nomic-embed-text)
ollama>=0.3

# T2.6 — local vector store (ADR-015)
lancedb>=0.5

# T2.7 — OCR fallback for scanned PDFs (PRD §9 locks OCR to Tesseract).
# pypdfium2 rasterizes pages (self-contained wheel — no Poppler); pytesseract
# wraps the Tesseract CLI and needs Pillow for the page images. NOTE: the
# Tesseract binary itself is a system prerequisite, e.g.
#   Windows: winget install --id UB-Mannheim.TesseractOCR
#   Debian/Ubuntu: apt install tesseract-ocr
pypdfium2>=4.0
pytesseract>=0.3.10
Pillow>=10.0
