/** Placeholder pillar — the real Search surface is W5. */
export function Search() {
  return (
    <div className="pillar-empty">
      <h1>Search</h1>
      <p>Find a line you half-remember, across your library.</p>
      <span className="hint">Arrives in W5.</span>
    </div>
  );
}
