/** Placeholder pillar — the real Ask surface (composer, streaming answer,
 *  evidence margin) is W3. W2 proves the frame, deliberately nothing more. */
export function Ask() {
  return (
    <div className="pillar-empty">
      <h1>Welcome back.</h1>
      <p>Ask anything about your documents.</p>
      <span className="hint">The composer and the evidence margin arrive in W3.</span>
    </div>
  );
}
