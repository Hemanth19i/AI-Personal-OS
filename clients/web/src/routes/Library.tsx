/** Placeholder pillar — the real Library surface is W4. */
export function Library() {
  return (
    <div className="pillar-empty">
      <h1>Library</h1>
      <p>Every document you have given the study, and its state.</p>
      <span className="hint">Arrives in W4.</span>
    </div>
  );
}
